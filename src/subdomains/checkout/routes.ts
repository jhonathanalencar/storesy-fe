import { CheckoutSuccessContainer } from './containers/checkout-success.container';
import { CheckoutContainer } from './containers/checkout.container';

export const checkoutRoutes = {
  CHECKOUT: CheckoutContainer,
  SUCCESS: CheckoutSuccessContainer,
};
