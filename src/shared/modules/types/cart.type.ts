export type TCartProduct = {
  product_id: string;
  product_slug: string;
  subtotal: number;
  product_url: string;
  product_title: string;
  product_quantity: number;
  quantity_available: number;
};
